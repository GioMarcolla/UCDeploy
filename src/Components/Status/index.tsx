import React, { FC } from "react";
import { Avatar, Circle, Flex, Icon, Text } from "@chakra-ui/react";

type Props = {
    currStatus: string;
};
let Status: FC<Props> = ({}: Props) => {
    return (
        <Flex>
            Status
        </Flex>)
};

export default Status;