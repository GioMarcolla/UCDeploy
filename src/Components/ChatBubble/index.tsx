import { Box } from "@chakra-ui/react";
import React, { FC } from "react";

type Props = {};

const ChatBubble: FC<Props> = (props: Props) => {
  return <Box>ChatBubble</Box>;
};

export default ChatBubble;
